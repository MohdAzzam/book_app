<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Book Collection</title>
  </head>
  <body>
    <main>
        <% searchResults.forEach( book => { %>
            <img src="<%=book.img%>" >
            <h3><%= book.title %></h3>
            <p><%= book.description %></p>
        <% }) %>
    </main>
  </body>
</html>